# v3.8.20 — Profile mobile polish

- mobile profile navigation changed from icon-only horizontal strip to visible 2×2 icon + label controls;
- profile navigation labels and counters remain visible without hover/tap;
- character guild-state badge is moved off the artwork on mobile and rendered at the far right of the name/realm header;
- non-guild mobile label is shortened to `Інший`;
- mobile character artwork reduced to 148px (136px on very narrow phones);
- character title/meta/stats/actions spacing tightened for better phone density;
- desktop profile/card behavior remains unchanged because all layout overrides are scoped to `max-width: 760px`.
