# v3.8.19 — Profile mobile normalization + raid signup cards

- mobile profile sidebar compacted: avatar/name/status stay in one horizontal identity block;
- profile facts, navigation, header actions and overview cards normalized for phone widths;
- character cards are shorter on phones and action buttons use a consistent touch grid;
- Battle.net blocks and profile counters receive mobile spacing normalization;
- profile raid signup cards were redesigned for both desktop and mobile;
- raid title/date/difficulty and metadata no longer collapse into concatenated text;
- desktop raid cards use a two-zone layout, while mobile cards stack into compact stat tiles;
- changes are scoped to the profile page and its raid list; unrelated desktop pages are untouched;
- v3.8.18 guild-mobile changes remain included.
